name: dart_envs
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.7.0
resolution: workspace

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
